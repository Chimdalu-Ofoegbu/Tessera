# Renaiss Index API — partner tier (optional; 10k req/day). Leave blank to use MockSource.
RENAISS_API_KEY=
RENAISS_API_SECRET=
# Set to 1 to use the real RenaissSource; unset/0 = MockSource (the demo-safe default).
USE_RENAISS=
