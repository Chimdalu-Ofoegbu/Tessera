/**
 * Placeholder shell. The production UI is built externally in Claude Design and
 * wired to this backend during the frontend-integration phase. Until then this
 * confirms the app compiles and the data layer / API are the real deliverables.
 */
export default function App() {
  return (
    <main style={{ padding: '2rem', maxWidth: 720, margin: '0 auto' }}>
      <h1>Tessera</h1>
      <p>Collector market-intelligence backend is online.</p>
      <p style={{ opacity: 0.7 }}>
        Renaiss gives you the price; Tessera adds the risk lens. UI arrives via the
        Claude Design handoff.
      </p>
    </main>
  )
}
