---
phase: 03-api-cache
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - api/overview.ts
  - api/categories.ts
  - api/categories/[id].ts
  - api/index/[id].ts
  - api/health.ts
  - tsconfig.json
  - README.md
autonomous: true
requirements: [API-01, SHIP-01]
must_haves:
  truths:
    - "Four JSON endpoints (overview, categories, category detail, index series) plus health are served by thin Vercel handlers"
    - "Every response is JSON with provenance on every metric and a cache-control header"
    - "An unknown category id returns 404 JSON (not a crash); thin data surfaces insufficient, not a bare number"
    - "The SPA rewrite (vercel.json) leaves /api/* reachable while deep-links survive refresh (SHIP-01 config)"
  artifacts:
    - path: "api/overview.ts"
      provides: "GET /api/overview handler"
      contains: "buildOverview"
    - path: "api/categories/[id].ts"
      provides: "GET /api/categories/:id detail handler"
      contains: "buildCategoryAnalytics"
    - path: "README.md"
      provides: "Public JSON API documentation (endpoints + shapes)"
      contains: "/api/overview"
---

<objective>
Expose the compute layer over a thin, cached Vercel `/api` — four JSON endpoints (overview, category list, category detail, index series) plus a health probe — that attach provenance to every metric and never do math themselves. Confirm the SPA rewrite keeps `/api/*` reachable while deep-links survive refresh, and document the public API (bonus ecosystem value). (API-01, SHIP-01)
</objective>

<execution_context>
@src/lib/compute.ts
@src/data/getDataSource.ts
@vercel.json
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Vercel handlers (api/*.ts)</name>
  <read_first>
    - src/lib/compute.ts (builders + payload types)
    - src/data/getDataSource.ts (the active source)
    - node_modules/@vercel/node (VercelRequest/VercelResponse signature)
  </read_first>
  <action>
    Thin handlers using `@vercel/node` (`export default function handler(req: VercelRequest, res: VercelResponse)`). Each: get `const source = getDataSource()`, call the compute builder, `res.setHeader('Cache-Control', 's-maxage=60, stale-while-revalidate=300')`, `res.setHeader('content-type','application/json')`, `res.status(200).json(payload)`. Wrap in try/catch → `res.status(500).json({ error })`. Import compute via relative path `../src/lib/compute` (or `../../` for nested).
    - `api/overview.ts` → `buildOverview(source, Date.now())`.
    - `api/categories.ts` → `buildOverview(...).categories` (the card list).
    - `api/categories/[id].ts` → read `req.query.id`; if unknown, catch the source error → `res.status(404).json({ error: 'unknown category', id })`; else `buildCategoryAnalytics(source, id, Date.now())`.
    - `api/index/[id].ts` → `buildCategoryAnalytics(...).indexSeries` for `req.query.id` (404 on unknown); honor `?window` if present (default 365).
    - `api/health.ts` → `source.health()`.
  </action>
  <acceptance_criteria>
    - `api/overview.ts` contains `buildOverview`; `api/categories/[id].ts` contains `buildCategoryAnalytics` and a `404` branch (grep each)
    - all handlers set a `Cache-Control` header (grep: `grep -rc "Cache-Control" api` >= 4)
    - `pnpm exec tsc` exits 0 with `api` in tsconfig include
  </acceptance_criteria>
  <done>Four endpoints + health, thin, cached, provenance-carrying, 404-safe.</done>
</task>

<task type="auto">
  <name>Task 2: tsconfig include api + README API docs</name>
  <read_first>
    - tsconfig.json
    - .planning/research/RENAISS-API.md (attribution wording for the data source)
  </read_first>
  <action>
    Add `"api"` to `tsconfig.json` `include`. Create `README.md` documenting: what Tessera is (one line), the public JSON API (`GET /api/overview`, `/api/categories`, `/api/categories/:id`, `/api/index/:id?window=`, `/api/health`) with example response shape noting the provenance envelope `{ value|insufficient, confidence, sampleSize, source, asOf }`, the `USE_RENAISS`/creds toggle, data attribution to the Renaiss Index API, and local run (`pnpm dev`, `vercel dev` for functions). Keep it concise and accurate.
  </action>
  <acceptance_criteria>
    - `tsconfig.json` include contains `"api"`; `pnpm build` exits 0
    - `README.md` contains `/api/overview` and `provenance` (grep each)
  </acceptance_criteria>
  <done>api/ typechecked; public API documented.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-0302-01 | DoS | Rate-limited upstream on each request | mitigate | Compute-layer TTL cache + Cache-Control s-maxage (CDN caches); MockSource default. |
| T-0302-02 | Info Disclosure | Error internals leaked | mitigate | Catch → generic `{ error }`; no secrets in responses; secrets are env-only (Phase 1). |
| T-0302-03 | Availability | Unknown id crashes function | mitigate | 404 JSON branch on unknown category; try/catch on every handler. |
| T-0302-04 | Correctness | SPA rewrite shadows /api | mitigate | Vercel auto-excludes /api/* from the SPA rewrite; documented + confirmed (SHIP-01 config). |
</threat_model>

<verification>
- `pnpm exec tsc` (with api included) and `pnpm build` exit 0.
- Handlers thin, cached, provenance-carrying, 404-safe; README documents the public API; vercel.json keeps /api reachable.
- Note: live endpoint responses are exercised on deploy (Phase 6, `vercel dev` / deployed URL); logic is covered by 03-01 compute tests.
</verification>

<success_criteria>
A thin, cached, documented public JSON API over the compute layer, with provenance on every metric and SPA-safe routing config. (API-01, SHIP-01)
</success_criteria>

<output>Create `.planning/phases/03-api-cache/03-02-SUMMARY.md` documenting the endpoints, handler signature, caching headers, and the SPA-rewrite confirmation.</output>
