---
phase: 03-api-cache
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/cache.ts
  - src/lib/compute.ts
  - src/lib/compute.test.ts
autonomous: true
requirements: [API-01]
must_haves:
  truths:
    - "The compute layer assembles overview + category-detail payloads from any DataSource + the pure engines"
    - "Every metric in a payload carries provenance (source, asOf, confidence, sampleSize) — no bare numbers"
    - "A thin category surfaces insufficient risk AND insufficient index in its payload (safety preserved end-to-end)"
    - "Payloads are memoized in an in-memory TTL cache (repeated calls return identical, instant results)"
  artifacts:
    - path: "src/lib/compute.ts"
      provides: "buildOverview + buildCategoryAnalytics wiring source→engines→payloads"
      contains: "computeRisk"
    - path: "src/lib/cache.ts"
      provides: "tiny TTL memo cache"
      contains: "ttl"
---

<objective>
Build the service layer that assembles the JSON API payloads: for each category, fetch normalized data from the active `DataSource`, run the pure `computeRisk` + `buildIndex` engines, and shape the exact structures the UI needs (overview cards + category detail), with provenance preserved on every metric and a thin category degrading to insufficient end-to-end. Memoize behind a small TTL cache so repeated requests are identical and instant. (API-01)
</objective>

<execution_context>
@src/data/DataSource.ts
@src/data/getDataSource.ts
@src/core/risk.ts
@src/core/indexEngine.ts
@tessera-ui-design-prompt.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: TTL memo cache (src/lib/cache.ts)</name>
  <read_first>
    - src/data/renaiss/client.ts (existing cache pattern for consistency)
  </read_first>
  <action>
    Create `src/lib/cache.ts`: `export async function memo<T>(key: string, ttlMs: number, now: number, fn: () => Promise<T>): Promise<T>` backed by a module-level `Map<string,{at:number,value:unknown}>`. Return cached value when `now − at < ttlMs`, else compute + store. Add `export function clearComputeCache(): void`.
  </action>
  <acceptance_criteria>
    - `src/lib/cache.ts` exports `memo` and `clearComputeCache`; contains `ttl` (grep)
    - `pnpm exec tsc` exits 0
  </acceptance_criteria>
  <done>A tiny, testable TTL memo cache.</done>
</task>

<task type="auto">
  <name>Task 2: Compute layer (src/lib/compute.ts)</name>
  <read_first>
    - src/data/DataSource.ts, src/data/schema.ts, src/data/metric.ts
    - src/core/risk.ts (RiskInput/RiskBreakdown), src/core/indexEngine.ts (IndexInput/IndexResult)
    - tessera-ui-design-prompt.md (Screens A/B — the exact fields the UI renders)
  </read_first>
  <action>
    Export payload types + builders. `now` is injected (default `Date.now()` in the handler, passed through for determinism in tests).
    - `CategoryCard = { id; game; label; index: Metric<number>; base; deltas; sparkline; updatedAt; constituentCount; risk: Metric<RiskBreakdown> }`.
    - `CategoryAnalytics = CategoryCard & { constituents; recentSales; floor: Metric<Money>; volume: Money; indexSeries: Metric<IndexResult> }`.
    - `Overview = { totalListings: number; totalVolume: Money; topMovers: Mover[]; categories: CategoryCard[]; source: SourceId; asOf: string }`.
    - `buildCategoryAnalytics(source, id, now): Promise<CategoryAnalytics>`: `getCategory(id)` (detail) + `getIndexSeries(id, 365)`; rawSeries = ok ? value : detail.sparkline; constituentValuesCents from priced constituents; run `computeRisk` + `buildIndex` using the detail.index provenance fields (source/asOf/confidence/sampleSize); floor = min priced constituent as `Metric<Money>` (insufficient if none); volume = Σ recentSales transaction prices.
    - `buildCategoryCard(...)`: project analytics → card (drop constituents/sales/floor/volume/indexSeries).
    - `buildOverview(source, now)`: for every `getCategories()` id, build the card; `totalListings` = Σ constituentCount; `totalVolume` = Σ per-category volume; `topMovers` = `getFeaturedMovers()`; `source`/`asOf` from the freshest card.
    - Wrap `buildOverview` and `buildCategoryAnalytics` in `memo(...)` (TTL 60s) keyed by `source-tag:id`.
    - Never emit a bare number: floor/risk/index are Metric-wrapped; a category with insufficient index yields insufficient risk + index.
  </action>
  <acceptance_criteria>
    - `src/lib/compute.ts` exports `buildOverview`, `buildCategoryAnalytics`, `buildCategoryCard` and references `computeRisk` + `buildIndex` (grep each)
    - `pnpm exec tsc` exits 0
  </acceptance_criteria>
  <done>Payload builders wire source→engines with provenance preserved + cached.</done>
</task>

<task type="auto">
  <name>Task 3: Compute tests (src/lib/compute.test.ts)</name>
  <read_first>
    - src/lib/compute.ts
    - src/data/mock/MockSource.ts (the source under test)
  </read_first>
  <action>
    Vitest, node env, against `MockSource` with a fixed `now`. Assert: (a) `buildOverview` returns 4 category cards; (b) every card's `index` and `risk` carry `provenance.source === 'seed'`; (c) the thin `lorcana` card has `risk.ok === false` AND (via detail) `indexSeries.ok === false`; (d) a liquid card (pokemon) has `risk.ok === true` with a reconciling breakdown and `index.ok === true`; (e) `buildCategoryAnalytics('sports')` floor is the min priced constituent; (f) `totalVolume.cents > 0` and `totalListings > 0`; (g) memo returns the same object reference within TTL.
  </action>
  <acceptance_criteria>
    - `pnpm exec vitest run src/lib` exits 0 with >= 6 passing assertions
    - test asserts thin category insufficient end-to-end (grep: `grep -c "lorcana" src/lib/compute.test.ts` >= 1)
  </acceptance_criteria>
  <done>Tests prove the payloads carry provenance, thin→insufficient end-to-end, and caching works.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-0301-01 | Integrity | Bare/unsourced number in a payload | mitigate | All metrics Metric-wrapped (provenance by type); thin → insufficient; tested. |
| T-0301-02 | DoS | Unbounded source calls (rate limits) | mitigate | 60s TTL memo cache in front of the source. |
| T-0301-03 | Correctness | Insufficiency lost during assembly | mitigate | Insufficient index → insufficient risk; end-to-end test on lorcana. |
</threat_model>

<verification>
- `pnpm exec tsc` and `pnpm exec vitest run src/lib` exit 0.
- Overview + detail payloads carry provenance on every metric; thin category insufficient end-to-end; cache memoizes.
</verification>

<success_criteria>
The compute layer assembles provenance-complete overview + detail payloads from any DataSource via the pure engines, with thin data degrading to insufficient and TTL caching. (API-01)
</success_criteria>

<output>Create `.planning/phases/03-api-cache/03-01-SUMMARY.md` documenting payload shapes, the source→engine wiring, cache TTL, and any deviations.</output>
