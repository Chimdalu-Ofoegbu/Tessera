---
phase: 01-data-layer-metric-contract
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/data/DataSource.ts
  - src/data/getDataSource.ts
  - src/data/mock/MockSource.ts
  - src/data/fixtures/categories.ts
  - src/data/fixtures/series.ts
  - src/data/fixtures/sales.ts
  - src/data/mock/MockSource.test.ts
autonomous: true
requirements: [DATA-01, PROV-02]
must_haves:
  truths:
    - "A single DataSource interface exposes categories, category detail, index series, recent sales, movers, and health"
    - "MockSource returns every value provenance-wrapped (source 'seed') and returns the insufficient branch for the thin category"
    - "Seed data includes a liquid, a momentum-spike, a concentrated, and a deliberately-thin category so every downstream branch has data"
    - "Nothing outside src/data/ imports fixtures (anti-corruption boundary)"
    - "getDataSource() returns MockSource by default"
  artifacts:
    - path: "src/data/DataSource.ts"
      provides: "The port: DataSource interface every source implements + Mover/Health types + Window"
      contains: "interface DataSource"
    - path: "src/data/mock/MockSource.ts"
      provides: "MockSource implements DataSource over typed fixtures; provenance source 'seed'"
      contains: "implements DataSource"
    - path: "src/data/getDataSource.ts"
      provides: "The SINGLE wiring point; returns MockSource by default"
      contains: "getDataSource"
    - path: "src/data/fixtures/categories.ts"
      provides: "Seed categories incl. thin/spike/concentrated shapes"
      contains: "pokemon"
  key_links:
    - from: "src/data/mock/MockSource.ts"
      to: "src/data/DataSource.ts"
      via: "implements the interface"
      pattern: "implements DataSource"
    - from: "src/data/getDataSource.ts"
      to: "src/data/mock/MockSource.ts"
      via: "factory returns MockSource by default"
      pattern: "new MockSource"
---

<objective>
Define the `DataSource` port — the one interface every source (mock now, Renaiss next) implements — and ship `MockSource` over in-repo typed fixtures so the whole app can be built and demoed on reliable, provenance-wrapped seed data. Seed the fixtures with four deliberately distinct market shapes (liquid, momentum-spike, concentrated, and a thin category that trips the insufficient branch) so every engine path (Phase 2) and the safety state have real data to exercise. Fixtures live behind `src/data/` and are imported by nothing outside it (anti-corruption boundary). `getDataSource()` is the single wiring point and returns `MockSource` by default. (DATA-01, PROV-02.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/ARCHITECTURE.md
@.planning/research/RENAISS-API.md
@.planning/PROJECT.md

<notes>
- Categories are identified by a string `id` (slug). The three real Renaiss games map 1:1 (`pokemon`, `one-piece`, `sports`); a fourth mock-only `lorcana` category is the deliberately-thin case (RenaissSource will only return the real three — that contrast is honest and demo-worthy).
- Provenance for all mock data: `source: 'seed'`, `asOf` = a fixed recent ISO date in the fixtures (NOT a render-time clock — freshness must be data-driven), `confidence`/`sampleSize` per fixture. Thin category: low confidence + tiny sampleSize → its `index` metric is `insufficient`.
- Every DataSource method returns normalized entities from schema.ts; parse fixtures through the zod schemas so a malformed fixture fails loudly in tests.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DataSource port (src/data/DataSource.ts)</name>
  <read_first>
    - src/data/schema.ts (entity + Window types the interface returns)
    - src/data/metric.ts (Metric envelope)
    - .planning/research/RENAISS-API.md (endpoint→method mapping table)
  </read_first>
  <action>
    Create `src/data/DataSource.ts`:
    - Import types from `./schema` and `./metric`.
    - `export interface Mover { id: string; label: string; deltaPct: number | null; index: Metric<number> }`.
    - `export interface Health { ok: boolean; source: SourceId }`.
    - `export interface DataSource {
        getCategories(): Promise<Category[]>;
        getCategory(id: string): Promise<CategoryDetail>;
        getIndexSeries(id: string, window: Window): Promise<Metric<PricePoint[]>>;
        getRecentSales(id: string, limit?: number): Promise<Sale[]>;
        getFeaturedMovers(limit?: number): Promise<Mover[]>;
        health(): Promise<Health>;
      }`.
    - Add a short doc comment: aggregate methods return the insufficient branch when data is too thin; `getCategory` throws only for an unknown id.
  </action>
  <acceptance_criteria>
    - `src/data/DataSource.ts` contains `export interface DataSource` and all six method names (grep each: getCategories, getCategory, getIndexSeries, getRecentSales, getFeaturedMovers, health).
    - Imports types from `./schema` and `./metric` (grep both).
    - `pnpm exec tsc -b` exits 0.
  </acceptance_criteria>
  <verify><automated>pnpm exec tsc -b</automated></verify>
  <done>The DataSource port exists with all six methods, typed against the normalized schema.</done>
</task>

<task type="auto">
  <name>Task 2: Seed fixtures with four market shapes (src/data/fixtures/*)</name>
  <read_first>
    - src/data/schema.ts (shapes fixtures must satisfy)
    - .planning/research/RENAISS-API.md (realistic field values: games, grades, USD cents, deltas)
  </read_first>
  <action>
    Create typed fixtures under `src/data/fixtures/` (plain TS exporting typed objects, parsed by schemas at load):
    - `categories.ts`: four categories —
      1. `pokemon` — LIQUID/healthy: many constituents, high confidence, large sampleSize, smooth upward sparkline, `index` metric `ok`.
      2. `one-piece` — MOMENTUM SPIKE: sparkline with a sharp recent jump; positive d7/d30; `index` metric `ok`, medium confidence.
      3. `sports` — CONCENTRATED: a handful of constituents where the top one holds a dominant share of value (rank 1 price ≫ others); `index` metric `ok`.
      4. `lorcana` — DELIBERATELY THIN: 1–2 constituents, `confidence: 'low'`, `sampleSize` below MIN_SAMPLE, sparse sparkline; its `index` metric is `insufficient(...)`.
      Each category includes `base: 100`, realistic `deltas`, `constituentCount`, `updatedAt` (fixed ISO), and constituents with `price` as `Metric<Money>` (the thin category's constituents may be insufficient).
    - `series.ts`: per-category `PricePoint[]` for windows (at least 30 & 90); the thin category has too few points (used to return insufficient index series).
    - `sales.ts`: per-category recent `Sale[]` (timestamped); thin category has 0–1 sales.
    Provenance on every metric: `source:'seed'`. Export helper maps keyed by category id.
  </action>
  <acceptance_criteria>
    - `src/data/fixtures/categories.ts` contains `pokemon`, `one-piece`, `sports`, and `lorcana` (grep each).
    - The thin category uses `insufficient(` for its index (grep: `grep -c "insufficient(" src/data/fixtures/categories.ts` >= 1).
    - Fixtures parse: a test (Task 4) parses every fixture through its schema with zero errors.
    - `grep -rc "fixtures" src/ --include=*.ts | grep -v "src/data/"` returns no matches (nothing outside src/data imports fixtures). [boundary check]
  </acceptance_criteria>
  <verify><automated>pnpm exec tsc -b</automated></verify>
  <done>Four distinct, schema-valid market shapes are seeded, including a thin category whose index is insufficient.</done>
</task>

<task type="auto">
  <name>Task 3: MockSource + getDataSource() factory</name>
  <read_first>
    - src/data/DataSource.ts (interface to implement)
    - src/data/fixtures/categories.ts (data to serve)
    - src/data/metric.ts (ok/insufficient constructors)
  </read_first>
  <action>
    Create `src/data/mock/MockSource.ts`: `export class MockSource implements DataSource`. Each method reads from the fixtures (parsed through schemas once at construction), returning provenance-wrapped values. `getIndexSeries` returns `insufficient(...)` when the category has fewer than MIN_SAMPLE points (import from `../../core/thresholds`). `getCategory` throws a clear error for an unknown id. `health()` returns `{ ok: true, source: 'seed' }`.
    Create `src/data/getDataSource.ts`: `export function getDataSource(): DataSource { /* default */ return new MockSource(); }` — the SINGLE wiring point. Leave a documented seam comment where 01-04 adds the RenaissSource branch. Nothing else in the app constructs a source directly; consumers call `getDataSource()`.
  </action>
  <acceptance_criteria>
    - `src/data/mock/MockSource.ts` contains `implements DataSource` and `source: 'seed'` (grep both).
    - `MockSource` imports thresholds (grep: `grep -c "thresholds" src/data/mock/MockSource.ts` >= 1) and returns `insufficient(` somewhere (grep >= 1).
    - `src/data/getDataSource.ts` contains `new MockSource` and exports `getDataSource` (grep both).
    - `pnpm exec tsc -b` exits 0.
  </acceptance_criteria>
  <verify><automated>pnpm exec tsc -b</automated></verify>
  <done>MockSource implements the full port over seed fixtures with provenance + insufficiency; getDataSource() returns it by default.</done>
</task>

<task type="auto">
  <name>Task 4: MockSource + fixture tests</name>
  <read_first>
    - src/data/mock/MockSource.ts
    - src/data/fixtures/categories.ts
  </read_first>
  <action>
    Create `src/data/mock/MockSource.test.ts` (Vitest):
    - Every fixture category parses through `CategorySchema`/`CategoryDetailSchema` with no error.
    - `getCategories()` returns 4 categories; each metric carries `provenance.source === 'seed'`.
    - The thin category (`lorcana`) `index.ok === false` (insufficient); `getIndexSeries('lorcana', 30)` returns `ok:false`.
    - A liquid category (`pokemon`) `index.ok === true` and `getIndexSeries('pokemon', 30).ok === true`.
    - `getCategory('does-not-exist')` rejects/throws.
    - `getDataSource()` returns an instance whose `health()` resolves `{ ok: true, source: 'seed' }`.
  </action>
  <acceptance_criteria>
    - `pnpm exec vitest run src/data` exits 0 with the new tests passing (>= 5 assertions across the MockSource suite).
    - Test asserts thin category is insufficient (grep: `grep -c "lorcana" src/data/mock/MockSource.test.ts` >= 1).
  </acceptance_criteria>
  <verify><automated>pnpm exec vitest run src/data</automated></verify>
  <done>Tests prove MockSource serves provenance-wrapped data, the thin category is insufficient, and the factory returns MockSource.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| fixtures -> app | Seed data must be validated and confined behind the data layer |
| source selection -> consumers | Consumers must not know which source is active |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Integrity | Malformed fixtures | mitigate | Fixtures parsed through zod schemas at load; a bad fixture fails tests loudly. |
| T-03-02 | Tampering (leaky abstraction) | Fixtures imported app-wide | mitigate | Anti-corruption boundary: nothing outside `src/data/` imports fixtures (grep-asserted); consumers use `getDataSource()`. |
| T-03-03 | Information Disclosure (false confidence) | Thin data shown as a number | mitigate | Thin category returns the `insufficient` branch (source-level), not a fabricated value. |
</threat_model>

<verification>
- `pnpm exec tsc -b` and `pnpm exec vitest run src/data` exit 0.
- DataSource port present with six methods; MockSource implements it over four seeded market shapes.
- Thin category returns insufficient at tile + series level; provenance source 'seed' on every value.
- No fixture import outside `src/data/`; getDataSource() returns MockSource by default.
</verification>

<success_criteria>
The single DataSource port exists and MockSource serves reliable, provenance-wrapped seed data with four distinct market shapes (incl. a thin insufficient case), confined behind the data layer, selected via getDataSource(). (DATA-01, PROV-02.)
</success_criteria>

<output>
Create `.planning/phases/01-data-layer-metric-contract/01-03-SUMMARY.md` documenting the DataSource method signatures, the four seeded categories and their shapes, the thin-category insufficiency, and the getDataSource() seam left for 01-04.
</output>
