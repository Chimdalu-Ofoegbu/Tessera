---
phase: 02-analytics-engines-risk-index
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/thresholds.ts
  - src/core/risk.ts
  - src/core/risk.test.ts
autonomous: true
requirements: [RISK-03, RISK-05]
must_haves:
  truths:
    - "computeRisk is a pure, deterministic function: identical inputs → identical score; result carries RISK_ENGINE_VERSION"
    - "The four weighted factor contributions sum to the headline score (reconciliation guarantee)"
    - "Thin/low-confidence inputs return the insufficient branch (RISK-05), never a fabricated score"
    - "No NaN/Infinity ever leaks (empty/degenerate inputs guarded to insufficient)"
  artifacts:
    - path: "src/core/risk.ts"
      provides: "computeRisk + factor functions per METHODOLOGY.md; returns Metric<RiskBreakdown>"
      contains: "RISK_ENGINE_VERSION"
    - path: "src/core/risk.test.ts"
      provides: "table-driven tests: liquid/volatile/concentrated/thin, reconciliation, determinism"
      contains: "reconcile"
---

<objective>
Implement the risk engine exactly as specified in `02-METHODOLOGY.md`: a pure, deterministic, versioned function that scores a category 0–100 from four transparent factors (Liquidity, Volatility, Concentration, Data confidence), returns a reconciling breakdown + confidence band, and yields `INSUFFICIENT_DATA` (not a fabricated number) when data is thin. This is Tessera's core innovation and the "no black box" guarantee. (RISK-03, RISK-05)
</objective>

<execution_context>
@.planning/phases/02-analytics-engines-risk-index/02-METHODOLOGY.md
@src/data/metric.ts
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Extend thresholds with engine constants</name>
  <read_first>
    - src/core/thresholds.ts
    - .planning/phases/02-analytics-engines-risk-index/02-METHODOLOGY.md (Shared thresholds table)
  </read_first>
  <action>
    Add to `src/core/thresholds.ts`: `MIN_POINT_SAMPLE = 2`, `RISK_ENGINE_VERSION = 'risk@1.0.0'`, `INDEX_ENGINE_VERSION = 'index@1.0.0'`. Keep `MIN_SAMPLE`, `MAX_STALE_DAYS`. Pure, no imports.
  </action>
  <acceptance_criteria>
    - `grep -c "RISK_ENGINE_VERSION\|INDEX_ENGINE_VERSION\|MIN_POINT_SAMPLE" src/core/thresholds.ts` >= 3
    - `pnpm exec tsc` exits 0
  </acceptance_criteria>
  <done>Threshold constants for both engines exist and are pure.</done>
</task>

<task type="auto">
  <name>Task 2: Implement computeRisk (src/core/risk.ts)</name>
  <read_first>
    - .planning/phases/02-analytics-engines-risk-index/02-METHODOLOGY.md (Risk score section — formulas + weights + band + insufficient rule)
    - src/data/metric.ts (Metric/ok/insufficient/Provenance/Confidence/SourceId)
    - src/data/schema.ts (PricePoint)
  </read_first>
  <action>
    Implement per METHODOLOGY exactly. Export types `RiskFactor { raw; weight; contribution }`, `RiskBreakdown { score; factors:{liquidity,volatility,concentration,dataConfidence}; band; version }`, `RiskInput { series: PricePoint[]; constituentValuesCents: number[]; sampleSize; confidence; asOf; source; now }`. Export `computeRisk(input): Metric<RiskBreakdown>`.
    - Factor fns (pure, exported for testing): `liquidityRisk`, `volatilityRisk`, `concentrationRisk`, `dataConfidenceRisk` with the exact normalizations + constants (TARGET_OBS=40, COV_CAP=0.40, HHI, base/sparse/stale penalties).
    - Weights: 0.30/0.30/0.25/0.15. `contribution = weight·raw`; `score = round(Σ contribution)`.
    - `band` per the mult formula; `staleDays` from `now − Date.parse(asOf)`.
    - Insufficient guard FIRST: `series.length < MIN_SAMPLE` OR `totalObs < MIN_SAMPLE` OR (`confidence==='low'` && `sampleSize < MIN_SAMPLE`) → `insufficient(prov)`. Guard degenerate math (mean 0, N 0) so no NaN/Infinity.
    - Provenance from `{ source, asOf, confidence, sampleSize }`. Stamp `version = RISK_ENGINE_VERSION`.
    - `round`/`round2` helpers; clamp to [0,100].
  </action>
  <acceptance_criteria>
    - `src/core/risk.ts` exports `computeRisk` and contains `RISK_ENGINE_VERSION` (grep both)
    - factor fns `liquidityRisk`, `volatilityRisk`, `concentrationRisk`, `dataConfidenceRisk` exported (grep each)
    - `pnpm exec tsc` exits 0
  </acceptance_criteria>
  <done>A pure, versioned computeRisk producing a reconciling breakdown + band + insufficient handling.</done>
</task>

<task type="auto">
  <name>Task 3: Risk engine tests (src/core/risk.test.ts)</name>
  <read_first>
    - src/core/risk.ts
    - .planning/phases/02-analytics-engines-risk-index/02-METHODOLOGY.md (Worked example)
  </read_first>
  <action>
    Vitest, node env. Cover: (a) determinism — same input twice → deep-equal result; (b) reconciliation — `Σ factor.contribution` rounds to `score`; (c) a concentrated input has higher concentration factor than a diffuse one; (d) a volatile series has higher volatility factor than a flat one; (e) a thin series (< MIN_SAMPLE points) OR low-confidence+tiny-sample → `ok === false` (insufficient); (f) result carries `version === 'risk@1.0.0'`; (g) no NaN — every factor.raw is a finite number in [0,100] on a normal input.
  </action>
  <acceptance_criteria>
    - `pnpm exec vitest run src/core` exits 0 with >= 6 passing risk assertions
    - test asserts reconciliation (grep: `grep -c "reconcile\|contribution" src/core/risk.test.ts` >= 1)
  </acceptance_criteria>
  <done>Tests pin determinism, reconciliation, factor ordering, insufficiency, versioning, and NaN-safety.</done>
</task>
</tasks>

<threat_model>
## STRIDE (pure compute — narrow surface)
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-0201-01 | Tampering / Integrity | Fabricated score on thin data | mitigate | Insufficient guard returns no score when thin/low-confidence (RISK-05), tested. |
| T-0201-02 | Denial of Service (correctness) | NaN/Infinity from degenerate inputs (empty series, div-by-zero) | mitigate | Guards on empty series, zero mean, N≤1; clamp to [0,100]; test asserts finite factors. |
| T-0201-03 | Repudiation | Non-reproducible score | mitigate | Pure + deterministic + versioned; determinism test. |
</threat_model>

<verification>
- `pnpm exec tsc` and `pnpm exec vitest run src/core` exit 0.
- computeRisk pure/deterministic/versioned; factors reconcile to score; thin → insufficient; no NaN.
</verification>

<success_criteria>
The risk engine computes a deterministic, versioned, reconciling 0–100 score with a confidence band and first-class insufficiency, exactly per METHODOLOGY.md. (RISK-03, RISK-05)
</success_criteria>

<output>Create `.planning/phases/02-analytics-engines-risk-index/02-01-SUMMARY.md` with the final factor formulas/constants as implemented, the reconciliation approach, and any deviations.</output>
