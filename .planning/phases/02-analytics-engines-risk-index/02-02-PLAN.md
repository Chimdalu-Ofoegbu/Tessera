---
phase: 02-analytics-engines-risk-index
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - src/core/indexEngine.ts
  - src/core/indexEngine.test.ts
autonomous: true
requirements: [IDX-02, IDX-03]
must_haves:
  truths:
    - "buildIndex is pure, deterministic, versioned; base period reads exactly 100"
    - "Index is reproducible by hand: value_t = 100 · price_t / price_base"
    - "Periods below MIN_POINT_SAMPLE render an explicit gap (null), never interpolated (IDX-03)"
    - "A thin series (no valid base / < MIN_SAMPLE) returns insufficient, not a fabricated line"
  artifacts:
    - path: "src/core/indexEngine.ts"
      provides: "vwap() + buildIndex() per METHODOLOGY.md; returns Metric<IndexResult>"
      contains: "INDEX_ENGINE_VERSION"
    - path: "src/core/indexEngine.test.ts"
      provides: "base=100, hand-reproduction, gap rendering, insufficiency, determinism"
      contains: "100"
---

<objective>
Implement the index engine per `02-METHODOLOGY.md`: a volume-weighted price index rebased to 100 at the base period, reproducible by hand, with explicit gaps for thin periods (no interpolation) and `INSUFFICIENT_DATA` for thin series. Pure, deterministic, versioned. (IDX-02, IDX-03)
</objective>

<execution_context>
@.planning/phases/02-analytics-engines-risk-index/02-METHODOLOGY.md
@src/data/metric.ts
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Implement vwap + buildIndex (src/core/indexEngine.ts)</name>
  <read_first>
    - .planning/phases/02-analytics-engines-risk-index/02-METHODOLOGY.md (Category index section)
    - src/data/metric.ts (Metric/ok/insufficient)
    - src/data/schema.ts (PricePoint)
    - src/core/thresholds.ts (MIN_SAMPLE, MIN_POINT_SAMPLE, INDEX_ENGINE_VERSION)
  </read_first>
  <action>
    Export types `IndexPoint { t; value: number | null; n }`, `IndexResult { base: number; basePrice: number; points: IndexPoint[]; current: number; version }`, `IndexInput { series: PricePoint[]; source; asOf; confidence; sampleSize; now? }`.
    - `export function vwap(points): number` = `Σ(usdCents·n)/Σ n` (0 when Σn=0).
    - `export function buildIndex(input): Metric<IndexResult>`:
      - Sort series chronologically by `t`.
      - basePrice = usdCents of the EARLIEST point with `n ≥ MIN_POINT_SAMPLE`. If none, or `series.length < MIN_SAMPLE`, or basePrice ≤ 0 → `insufficient(prov)`.
      - points: `value = n ≥ MIN_POINT_SAMPLE ? round2(100 · usdCents / basePrice) : null` (gap).
      - `base = 100`; `current` = last non-null value.
      - Provenance from `{ source, asOf, confidence, sampleSize }`; `version = INDEX_ENGINE_VERSION`.
      - Guard against NaN/Infinity.
  </action>
  <acceptance_criteria>
    - `src/core/indexEngine.ts` exports `buildIndex` and `vwap`, contains `INDEX_ENGINE_VERSION` (grep each)
    - `pnpm exec tsc` exits 0
  </acceptance_criteria>
  <done>vwap + buildIndex implemented, base=100, gaps, insufficiency, versioned.</done>
</task>

<task type="auto">
  <name>Task 2: Index engine tests (src/core/indexEngine.test.ts)</name>
  <read_first>
    - src/core/indexEngine.ts
    - .planning/phases/02-analytics-engines-risk-index/02-METHODOLOGY.md (Worked example: [200000,210000,240000] → [100,105,120])
  </read_first>
  <action>
    Vitest, node env. Cover: (a) base period reads exactly 100 (`points[0].value === 100`); (b) hand reproduction — series `[200000,210000,240000]` (n=4) → values `[100, 105, 120]`, current 120; (c) a point with `n < MIN_POINT_SAMPLE` renders `value === null` (gap) and is NOT interpolated; (d) a series shorter than MIN_SAMPLE (or all-thin) → `ok === false` (insufficient); (e) determinism — same input twice deep-equal; (f) `version === 'index@1.0.0'`; (g) vwap weights by n correctly on a hand case.
  </action>
  <acceptance_criteria>
    - `pnpm exec vitest run src/core` exits 0 with >= 6 passing index assertions
    - test asserts base=100 and a null gap (grep: `grep -c "null" src/core/indexEngine.test.ts` >= 1)
  </acceptance_criteria>
  <done>Tests pin base=100, hand-reproduction, gaps, insufficiency, determinism, versioning, vwap.</done>
</task>
</tasks>

<threat_model>
## STRIDE (pure compute)
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-0202-01 | Integrity | Fabricated/interpolated index on thin data | mitigate | Gaps (null) for thin points; insufficient for thin series (IDX-03), tested. |
| T-0202-02 | Correctness | Base-period drift / div-by-zero | mitigate | Base = earliest point with n ≥ MIN_POINT_SAMPLE; guard basePrice ≤ 0 → insufficient. |
| T-0202-03 | Repudiation | Non-reproducible index | mitigate | Pure + deterministic + versioned; hand-reproduction + determinism tests. |
</threat_model>

<verification>
- `pnpm exec tsc` and `pnpm exec vitest run src/core` exit 0.
- base=100; reproducible by hand; gaps not interpolated; thin → insufficient; deterministic + versioned.
</verification>

<success_criteria>
The index engine produces a reproducible VWAP→100 index with explicit gaps and first-class insufficiency, exactly per METHODOLOGY.md. (IDX-02, IDX-03)
</success_criteria>

<output>Create `.planning/phases/02-analytics-engines-risk-index/02-02-SUMMARY.md` with the final formulas as implemented, the base-period rule, gap handling, and any deviations.</output>
